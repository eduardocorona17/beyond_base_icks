# This is Pythonic but gross
spam = eggs = bacon = 'string'
# spam == eggs == bacon == 'string'
print(spam, eggs, bacon)

# Chain them using '==' to see if all 3 vars are the same

# spam == eggs == bacon = 'string'  # Actually does not work here but does in inter. ac. shell
listaso = [num**2 for num in range(10)]
print(f"{[num**2 for num in range(10)]}")

